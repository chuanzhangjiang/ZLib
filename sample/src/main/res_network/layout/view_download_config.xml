<?xml version="1.0" encoding="utf-8"?>
<TableLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:orientation="vertical">
    <TableRow
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <TextView android:id="@+id/tv_url"
            android:text="@string/network_tv_text_url" />
        <EditText
            android:id="@+id/et_url"
            android:hint="@string/network_et_hint_url"
            android:inputType="textUri"
            android:layout_weight="1"/>
    </TableRow>

    <TableRow>
        <TextView android:id="@+id/tv_filename"
            android:text="@string/network_tv_text_filename"/>
        <EditText android:id="@+id/et_filename"
            android:hint="@string/network_et_hint_filename"
            android:layout_weight="1"/>
    </TableRow>
    <TableRow>
        <TextView android:id="@+id/tv_path"
            android:text="@string/network_tv_text_path"/>
        <EditText android:id="@+id/et_path"
            android:hint="@string/network_et_hint_path"
            android:layout_weight="1"/>
    </TableRow>
    <Button android:id="@+id/btn_download"
        android:text="@string/network_btn_text_download"/>
</TableLayout>