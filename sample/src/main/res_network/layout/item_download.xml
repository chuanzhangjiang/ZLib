<?xml version="1.0" encoding="utf-8"?>
<TableLayout xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <ProgressBar android:id="@+id/pb_download_progress"
        style="?android:attr/progressBarStyleHorizontal" />

    <TableRow
        android:layout_marginTop="@dimen/activity_horizontal_margin"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <Button android:id="@+id/btn_pause"
            android:layout_weight="1"
            android:text="@string/network_btn_text_pause"
            tools:ignore="ButtonStyle" />
        <Button android:id="@+id/btn_start"
            android:layout_weight="1"
            android:text="@string/network_btn_text_start"
            tools:ignore="ButtonStyle" />
        <Button android:id="@+id/btn_cancel"
            android:layout_weight="1"
            android:text="@string/network_btn_text_cancel"
            tools:ignore="ButtonStyle" />
    </TableRow>
</TableLayout>